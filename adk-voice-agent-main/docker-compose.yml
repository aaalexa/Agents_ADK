services:
  adk-voice-agent:
    build: .
    ports:
      - "8001:8001"
    volumes:
      - ./app/credentials.json:/app/app/credentials.json
      - ./.credentials:/root/.credentials
    environment:
      - PYTHONUNBUFFERED=1